ExpL specification stipulates that variables of [user defined types](https://silcnitc.github.io/expl-docs/expl/#nav-user-defined-types) are allocated dynamically using the alloc() function. The alloc() function has the following syntax:

`user_defined_type_var = alloc();`

